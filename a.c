int add(int a, int b)
{
    int x, y;

    a = !a;
    b = *(++p);
    b = *p++;
    x = (a + b) * 2, y = a - b;
    if (x == y)
        return a + b;
    else
        return a - b;
    (a == b) ? 1 : 0;
    a[10];
    foo(1,2,3);
}
